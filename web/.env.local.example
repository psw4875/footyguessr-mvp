# FootyGuessr Frontend Environment Variables
# Copy this file to .env.local and update values as needed

# Production backend: HTTPS API endpoint
# Used for REST API calls (/api/questions, /health, etc.)
NEXT_PUBLIC_SERVER_URL=https://api.footyguessr.io

# Socket.IO WebSocket endpoint (can be same as SERVER_URL)
# If not set, falls back to NEXT_PUBLIC_SERVER_URL
NEXT_PUBLIC_SOCKET_URL=https://api.footyguessr.io

# Local development (optional):
# NEXT_PUBLIC_SERVER_URL=http://localhost:4000
# NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
